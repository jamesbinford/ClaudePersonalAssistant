# Anthropic API Key (required)
# Get your key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Email recipient (required)
# Where to send the reminder emails
RECIPIENT_EMAIL=your_email@example.com

# ============================================
# OPTION 1: Local Mailpit (recommended)
# ============================================
# Run: docker compose up -d
# Web UI: http://localhost:8025
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=

# Mailpit relay settings (to actually send emails via Gmail)
# Set RELAY_ALL=true to forward all emails through Gmail
RELAY_HOST=smtp.gmail.com
RELAY_PORT=587
RELAY_USER=your_email@gmail.com
RELAY_PASSWORD=your_app_password_here
RELAY_ALL=true

# ============================================
# OPTION 2: Direct Gmail SMTP
# ============================================
# Uncomment these and comment out Option 1 above
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password_here

# Optional: Override the "From" address (defaults to SMTP_USER or RELAY_USER)
# FROM_EMAIL=notifications@example.com
